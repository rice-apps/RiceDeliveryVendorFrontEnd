import { types } from 'mobx-state-tree'

// export const SizePrice = types.model('SizePrice', {size: types.string, price: types.number})

// export const MenuItem = types
// .model('MenuItem', {
//     id: types.string,
//     name: types.string,
//     description: types.string,
//     inventory: types.number,
//     prices: types.array(SizePrice)
// })

// export const Location = types.model('Location', {name: types.string})

